import styled from "styled-components";

const StyledListExpenses = styled.ul`
  list-style-type: none;
  margin-top: 30px;
`;

const Empty = styled.p`
  text-align: center;
  font-size: 20px;
  font-weight: 400;
  line-height: 24px;
`;

export { StyledListExpenses, Empty };
