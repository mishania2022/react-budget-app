import styled from "styled-components";
import { Color } from "../../config/colors";
export const StyledItemExpenses = styled.li`
  width: 100%;
  height: 50px;
  box-shadow: 0px 4px 4px ${Color.Gray};
  margin-bottom:10px
`;
